/*
    ### Modules ###
        - Every file and folder makes its own separate
        module
        - Can't do deeply nested imports (can't skip
        levels)
        - Whenever we make a folder we need to add
        'mod.rs' to it?
*/

// 'pub' keyword exports module; 'mod' keyword imports
// module
pub mod catalog;
pub mod media;
